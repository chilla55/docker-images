version: '3.8'

services:
  redis:
    image: ${DOCKER_REGISTRY:-local}/redis:${VERSION:-latest}
    hostname: redis
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD}
    volumes:
      - redis-data:/data
    networks:
      - redis-net
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.labels.redis.node == node1
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
      resources:
        limits:
          cpus: '1'
          memory: 512M
        reservations:
          cpus: '0.5'
          memory: 256M

volumes:
  redis-data:
    driver: local

networks:
  redis-net:
    external: true
